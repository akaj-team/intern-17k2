<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="@dimen/cardview.radius"
    app:contentPadding="@dimen/cardview.radius"
    android:layout_margin="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvDate"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:layout_weight="2"
            tool:text="@string/app_name" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginEnd="@dimen/linearlayout.margin"
            android:layout_marginLeft="@dimen/linearlayout.margin"
            android:layout_marginRight="@dimen/linearlayout.margin"
            android:layout_marginStart="@dimen/linearlayout.margin"
            android:layout_weight="4"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="1"
                tool:text="@string/app_name" />

            <TextView
                android:id="@+id/tvContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="2"
                tool:text="@string/app_name" />
        </LinearLayout>

        <ImageView
            android:id="@+id/imgNote"
            android:layout_width="0dp"
            android:layout_height="@dimen/imageview.size.note"
            android:layout_weight="1"
            android:contentDescription="@null"
            android:scaleType="centerCrop" />
    </LinearLayout>

</android.support.v7.widget.CardView>
