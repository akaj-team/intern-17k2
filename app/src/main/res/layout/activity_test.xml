<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/test.ll.height"
        android:background="@color/colorYellow"
        android:gravity="center_vertical"
        android:padding="@dimen/test.ll.padding">

        <TextView
            android:id="@+id/tvPrev"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="@string/test.textView.back"
            android:textColor="@color/colorBlack"
            android:textSize="@dimen/test.textView.size"
            android:visibility="invisible" />

        <TextView
            android:id="@+id/tvQuestionTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="6"
            android:gravity="center_horizontal"
            android:text="@string/test.textView.question"
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/test.textView.size" />

        <TextView
            android:id="@+id/tvNext"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2.5"
            android:gravity="end"
            android:text="@string/test.textView.next"
            android:textColor="@color/colorBlack"
            android:textSize="@dimen/test.textView.size" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="@dimen/test.view.margin_top"
        android:background="@color/colorYellow" />

    <android.support.v4.view.ViewPager
        android:id="@+id/testViewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/test.viewPager.margin_top" />
</LinearLayout>
