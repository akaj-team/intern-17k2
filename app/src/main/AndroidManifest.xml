<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.asiantech.internship">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".drawer.ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".drawer.ui.main.MainActivity" />
        <activity android:name=".drawer.ui.feed.FeedActivity" />
        <activity android:name=".note.ui.NoteActivity" />
        <activity android:name=".day16.ui.TabActivity" />
        <activity android:name=".day7.ui.CommunicateActivity" />
        <activity android:name=".friend.FriendActivity" />
        <activity android:name=".day13.ChatActivity" />
        <activity android:name=".day15.ui.QuestionActivity" />
    </application>

</manifest>