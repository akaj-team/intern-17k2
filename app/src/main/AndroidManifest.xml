<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.asiantech.internship">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.UpLoadImageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".friend.Day5MainActivity" />
        <activity android:name=".friend.ListFriendActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".feed.FeedActivity" />
        <activity android:name=".ui.main.NoteActivity" />
        <activity android:name=".ui.main.PassDataActivity" />
        <activity android:name=".ui.main.ChatActivity" />
        <activity android:name=".ui.main.TestActivity" />
        <activity android:name=".ui.main.ResultActivity" />
        <activity android:name=".ui.main.FootballStarActivity" />
    </application>
</manifest>
